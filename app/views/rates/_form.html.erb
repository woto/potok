<%= form_with(model: rate, local: true) do |form| %>
  <% if rate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rate.errors.count, "error") %> prohibited this rate from being saved:</h2>

      <ul>
      <% rate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :from_currency %>
    <%= form.number_field :from_currency %>
  </div>

  <div class="field">
    <%= form.label :to_currency %>
    <%= form.number_field :to_currency %>
  </div>

  <div class="field">
    <%= form.label :buy %>
    <%= form.text_field :buy %>
  </div>

  <div class="field">
    <%= form.label :sell %>
    <%= form.text_field :sell %>
  </div>

  <div class="field">
    <%= form.label :receive_date %>
    <%= form.date_select :receive_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
